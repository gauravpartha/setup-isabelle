name: CI
on:
  pull_request:
  push:

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest]
        isabelle-version: [2024]
        #os: [ubuntu-22.04, ubuntu-latest]
        #isabelle-version: [2022, 2023, 2024]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Isabelle
        uses: ./
        with: 
          isabelle-version: ${{matrix.isabelle-version}}
      - name: Isabelle version
        run: isabelle version
      - name: Test expected version
        shell: bash
        run: |
          if [[ "$(isabelle version)" == "${{matrix.isabelle-version}}" ]]; then
            echo "Success"
            exit 0
          else
            echo "Incorrect Isabelle version"
            exit 1
          fi